# Copie para .env e preencha antes de rodar `docker compose up`.
# .env nunca deve ser commitado (já está no .gitignore).

POSTGRES_PASSWORD=troque-esta-senha
JWT_KEY=troque-por-uma-chave-aleatoria-de-pelo-menos-32-caracteres

# Opcional: usuário Admin criado automaticamente no primeiro start
DEV_ADMIN_EMAIL=admin@ecommerce.local
DEV_ADMIN_PASSWORD=TrocarEssaSenha@123
